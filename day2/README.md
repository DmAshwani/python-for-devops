# Day 2 — File & System Operations

- `log_analyzer.py` — parse logs, filter by level/pattern, export results
- `exercises_day2.py` — minimal examples for file I/O and subprocess usage

Example:
`python log_analyzer.py --dir ./logs --level ERROR --pattern timeout --export errors.log`
